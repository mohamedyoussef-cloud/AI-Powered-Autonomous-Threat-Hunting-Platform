# Deployment Notes

The initial academic prototype can run locally without cloud infrastructure.

MVP deployment:

- React frontend locally
- FastAPI backend locally
- PostgreSQL locally or Docker
- Redis locally or Docker
- Mock SIEM connectors
- Optional local LLM through Ollama
